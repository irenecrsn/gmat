# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: true 
cache: packages
branches:
    only:
        - master
os:
    - linux
    - osx
r:
    - oldrel
    - release
    - devel
install:
  - if [ $TRAVIS_OS_NAME = osx ]; then brew install libgit2; fi
before_script:
  - if [ $TRAVIS_OS_NAME = osx ]; then Rscript -e 'install.packages(c("igraph", "testthat"))'; fi
