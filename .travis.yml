# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages
branches:
    only:
        - master
os:
    - linux
    - osx
r:
    - oldrel
    - release
    - devel
